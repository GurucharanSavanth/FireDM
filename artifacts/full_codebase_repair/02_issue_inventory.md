# 02 Issue Inventory

Evidence labels: observed = local file/command output; inferred = local-code reasoning.

| ID | Sev | Category | Evidence | Behavior | Root Cause | Proposed Fix | Test/Validation | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FCR-001 | P1 | browser/native messaging | observed `firedm/plugins/browser_integration.py` manifest uses `"path": sys.executable` while `host_path` is unused | Browser launches Python/app without `firedm/native_host.py` | manifest path does not point at a native-host launcher | resolve real native-host executable/launcher; add source/package entry point | `tests/test_browser_integration.py`; source/package smoke | fixed |
| FCR-002 | P1 | local API security | observed `Controller.__init__` starts socket/pipe unconditionally and `_handle_native_message` trusts raw JSON | local control endpoint exposed by default | endpoint not gated behind enabled plugin; no controller-side auth | start endpoint only when browser plugin enabled; use authenticated local transport; validate payload | `tests/test_browser_integration.py`; full pytest | fixed |
| FCR-003 | P1 | Windows native pipe | observed `_named_pipe_loop` calls `os.open(r'\\.\pipe\FireDM_Controller', os.O_RDONLY)` | opens as client; no pipe server exists | named pipe created with wrong API | use stdlib `multiprocessing.connection.Listener/Client` server/client transport | listener smoke; full pytest | fixed |
| FCR-004 | P1 | test dependency / prohibited DRM | observed full pytest fails `ModuleNotFoundError: No module named 'cryptography'`; observed `drm_decryption.py` fetches ClearKey and decrypts DASH/HLS | test fails in clean env; code conflicts with no-DRM-bypass boundary | plugin adds DRM decryption path and optional undeclared crypto dep | quarantine active DRM decrypt behavior and update tests to assert unsupported | `tests/test_drm_clearkey.py`; full pytest | fixed |
| FCR-005 | P2 | magnet queue routing | observed `_magnet()` returns True after aria2 addUri but sets no `_plugin_queued` or `_plugin_completed`; controller then enqueues normal worker path | magnet downloads fail/retry through pycurl | plugin does not signal ownership of async aria2 job | mark `_plugin_queued`, set pending status, monitor completion with plugin-owned finalization | `tests/test_plugins.py::test_magnet_is_owned_by_plugin_queue`; full pytest | fixed |
| FCR-006 | P2 | native-host stdio correctness | observed `firedm/native_host.py` imports `utils.log` and logs controller-send failures while stdout is native messaging protocol | log text can corrupt browser native messaging stream | stdout logger used in native host process | replace with stderr-only diagnostics and binary protocol-only stdout | native-host smoke; browser tests | fixed |
| FCR-007 | P2 | plugin packaging drift | observed `scripts/firedm-win.spec` hiddenimports include `cryptography`, `paramiko` while pyproject does not declare them | PyInstaller may fail or collect absent optional deps inconsistently | optional plugin deps added to spec not dependency policy | remove prohibited/undeclared deps from required hiddenimports; keep plugins packaged | isolated PyInstaller build + packaged smoke | fixed |
| FCR-008 | P2 | unsafe subprocess strings | observed `post_processing.py` builds command strings for ClamAV/unrar/7z/ffmpeg and passes to `run_command` | user-controlled paths flow into command strings | plugin uses shell-style string construction | convert plugin subprocess calls to argv if kept, or document deferred if plugin remains disabled | targeted tests | deferred |
| FCR-009 | P2 | class/inheritance smell | observed `firedm/video.py::Key.__init__` uses `super().__init__(self)` | constructor likely passes self as segment name | legacy class bug; not currently touched by reviewer path | inspect/fix only with focused HLS key tests | class review | deferred |
| FCR-010 | P3 | legacy broad exceptions | observed many `except:`/`except Exception` across legacy modules | hides failure detail in large old surfaces | historical Tk/downloader style | fix only touched paths; defer large legacy cleanup | scoped lint/tests | deferred |

Severity summary after patches: fixed P1=4, fixed P2=3, deferred P2=2, deferred P3=1.
